# MovesCode Storage
Secure image, file and media storage for MovesOS. Install with `composer require movescode/storage:^1.0`. Classes `Image`, `File`, `Media` and `Send` validate actual MIME types before storing uploads. Requires PHP 8.2, Fileinfo, GD and Mbstring.
